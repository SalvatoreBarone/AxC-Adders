sudo: required
language: CXX
before_install:
        - sudo apt-get -qq update
        - sudo apt-get install -y cmake ninja-build
        - mkdir build
        - cd build
        - cmake .. -G Ninja
install:
        - ninja
script:
        - ls libs/libInexactAdders.a
